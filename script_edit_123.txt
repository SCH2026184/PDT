{"AllowMultiple":"false","Description":"\"Adicionando a função updateRulerTooltip e a escuta ao input mag-declination no script.js. Isso possibilita a rotação automática do mapa, atualização dos indicadores de norte, recálculo de impactos e da proa magnética da régua em tempo real conforme a declinação é alterada no toolbar.\"","EndLine":"1725","Instruction":"\"Insert updateRulerTooltip and mag-declination input event listener after ruler variable definitions\"","ReplacementContent":"\"    // Ruler Tool \u0026 Drawing Tool\\n    let rulerActive = false;\\n    let rulerPoints = [];\\n    let rulerPolyline = null;\\n    let rulerMarkers = [];\\n    let targetDragHandles = [];\\n\\n    function updateRulerTooltip() {\\n        if (!rulerPolyline || rulerPoints.length \u003c 2) return;\\n        let totalMeters = 0;\\n        for (let i = 0; i \u003c rulerPoints.length - 1; i++) {\\n            totalMeters += rulerPoints[i].distanceTo(rulerPoints[i+1]);\\n        }\\n        const totalFt = totalMeters * 3.28084;\\n        const startPt = rulerPoints[0];\\n        const endPt = rulerPoints[rulerPoints.length - 1];\\n        const bearing = getBearing(startPt, endPt);\\n        \\n        const dec = parseFloat(magDeclinationInput ? magDeclinationInput.value : 0) || 0;\\n        const magBearing = Math.round((bearing - dec + 360) % 360);\\n        \\n        let labelText = `${Math.round(totalMeters)} m / ${Math.round(totalFt)} ft | Eixo: ${String(magBearing).padStart(3, \u00270\u0027)}° (MAG)`;\\n        rulerPolyline.setTooltipContent(labelText);\\n    }\\n\\n    if (magDeclinationInput) {\\n        magDeclinationInput.addEventListener(\u0027input\u0027, () =\u003e {\\n            const dec = parseFloat(magDeclinationInput.value) || 0;\\n            if (map \u0026\u0026 map.setBearing) {\\n                map.setBearing(-dec);\\n            }\\n            drawImpactsOnMap();\\n            if (typeof renderCompassSVG === \u0027function\u0027) renderCompassSVG();\\n            updateNorthIndicators();\\n            updateRulerTooltip();\\n        });\\n    }\"","StartLine":"1719","TargetContent":"\"    // Ruler Tool \u0026 Drawing Tool\\n    let rulerActive = false;\\n    let rulerPoints = [];\\n    let rulerPolyline = null;\\n    let rulerMarkers = [];\\n    let targetDragHandles = [];\"","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Adding declination event listener and ruler syncer in script.js\"","toolSummary":"\"Add declination listener and ruler syncer\""}