{"Description":"\"Porting the advanced coordinate parser (Phase 3). Replacing the basic comma-based coordinate parser with a robust regex-based parser that handles spaces, concatenated coordinates, MGRS, DMM, DMS, and Decimal formats.\"","Instruction":"\"Replace parseInputCoords with an advanced parser\"","ReplacementChunks":"[{\"AllowMultiple\":false,\"EndLine\":689,\"ReplacementContent\":\"    function parseInputCoords(input) {\\n        if (!input) return null;\\n        \\n        // Helper to format string and remove excessive whitespace\\n        input = input.trim().toUpperCase();\\n\\n        const format = coordFormatSelect ? coordFormatSelect.value : null;\\n        if (format === \u0027MGRS\u0027) {\\n            try { \\n                const cleanInput = input.replace(/\\\\s+/g, \u0027\u0027);\\n                const lonlat = mgrs.toPoint(cleanInput); \\n                return { lat: lonlat[1], lng: lonlat[0] }; \\n            } catch(e) { return null; }\\n        }\\n\\n        // Tenta encontrar dois blocos de coordenadas, separados por vírgula ou espaço(s)\\n        // RegEx lida com Graus (DMS, DMM, Decimal) com vários símbolos como °, \u0027, \\\", ou texto\\n        // e opcionalmente pega o indicador N/S/E/W no início ou no fim.\\n        const parseAngle = (str) =\u003e {\\n            str = str.replace(/,/, \u0027.\u0027); // Troca vírgula decimal por ponto\\n            let val = NaN;\\n            let dir = \u0027\u0027;\\n\\n            // DMS: N 16° 01\u0027 26.5\\\"\\n            const dmsPrefix = str.match(/([NSEWO])\\\\s*(\\\\d+)[°\\\\s-]+(\\\\d+)[\u0027\\\\s-]+([\\\\d.]+)\\\"?/i);\\n            const dmsPostfix = str.match(/(\\\\d+)[°\\\\s-]+(\\\\d+)[\u0027\\\\s-]+([\\\\d.]+)\\\"?\\\\s*([NSEWO])/i);\\n            \\n            // DMM: N 16° 01.441\u0027\\n            const dmmPrefix = str.match(/([NSEWO])\\\\s*(\\\\d+)[°\\\\s-]+([\\\\d.]+)\u0027?/i);\\n            const dmmPostfix = str.match(/(\\\\d+)[°\\\\s-]+([\\\\d.]+)\u0027?\\\\s*([NSEWO])/i);\\n            \\n            // Decimal Degrees\\n            const decPrefix = str.match(/([NSEWO])\\\\s*([\\\\d.]+)/i);\\n            const decPostfix = str.match(/([\\\\d.]+)\\\\s*([NSEWO])/i);\\n            const decPure = str.match(/(-?[\\\\d.]+)/);\\n\\n            if (dmsPrefix) {\\n                val = parseInt(dmsPrefix[2]) + (parseInt(dmsPrefix[3]) / 60) + (parseFloat(dmsPrefix[4]) / 3600);\\n                dir = dmsPrefix[1];\\n            } else if (dmsPostfix) {\\n                val = parseInt(dmsPostfix[1]) + (par\n\u003ctruncated 6313 bytes\u003e","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Replacing the broken `parseInputCoords` in `script.js` with the robust advanced version we\u0027ve developed\"","toolSummary":"\"Port advanced coordinate parser\""}