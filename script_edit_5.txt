{"AllowMultiple":"false","Description":"\"Update TGT double click behavior in renderTgtList\"","EndLine":"1786","Instruction":"\"Make double click fly the map to the target without changing the zoom, and show a prompt to edit the coordinates manually.\"","ReplacementContent":"\"            item.addEventListener(\u0027dblclick\u0027, () =\u003e {\\n                map.flyTo(tgt.latlng, map.getZoom(), { duration: 1.0 });\\n                setTimeout(() =\u003e {\\n                    const currentCoordStr = formatCoords(tgt.latlng.lat, tgt.latlng.lng);\\n                    const newCoord = prompt(\\\"Editar coordenada do alvo:\\\", currentCoordStr);\\n                    if (newCoord !== null) {\\n                        const parsed = parseInputCoords(newCoord);\\n                        if (parsed) {\\n                            const newLatLng = L.latLng(parsed.lat, parsed.lng);\\n                            tgt.latlng = newLatLng;\\n                            tgt.marker.setLatLng(newLatLng);\\n                            tgt.coord = formatCoords(parsed.lat, parsed.lng);\\n                            \\n                            savedEmployments.forEach(emp =\u003e {\\n                                if (emp.linkedTgtId === tgt.id) {\\n                                    emp.targetLatLng = newLatLng;\\n                                }\\n                            });\\n                            \\n                            renderTgtList();\\n                            drawImpactsOnMap();\\n                            map.panTo(newLatLng);\\n                        } else {\\n                            alert(\\\"Formato de coordenada inválido.\\\");\\n                        }\\n                    }\\n                }, 50);\\n            });\"","StartLine":"1784","TargetContent":"\"            item.addEventListener(\u0027dblclick\u0027, () =\u003e {\\n                map.flyTo(tgt.latlng, 15, { duration: 1.5 });\\n            });\"","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Updating double click behavior\"","toolSummary":"\"Update TGT double click in renderTgtList\""}