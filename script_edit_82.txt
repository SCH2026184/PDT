{"Description":"\"Updating the ruler\u0027s distance calculation label to display the Magnetic Heading (MAG) instead of True Heading, taking into account the current magnetic declination.\"","Instruction":"\"Replace ruler label text to include magnetic declination adjustment\"","ReplacementChunks":"[{\"AllowMultiple\":false,\"EndLine\":2459,\"ReplacementContent\":\"                const bearing = getBearing(startPt, endPt);\\n                \\n                const dec = parseFloat(document.getElementById(\u0027mag-declination\u0027) ? document.getElementById(\u0027mag-declination\u0027).value : 0) || 0;\\n                const magBearing = Math.round((bearing - dec + 360) % 360);\\n                \\n                let labelText = `${Math.round(totalMeters)} m / ${Math.round(totalFt)} ft | Eixo: ${String(magBearing).padStart(3, \u00270\u0027)}° (MAG)`;\\n                \\n                rulerPolyline.setTooltipContent(labelText);\",\"StartLine\":2455,\"TargetContent\":\"                const bearing = getBearing(startPt, endPt);\\n                \\n                let labelText = `${Math.round(totalMeters)} m / ${Math.round(totalFt)} ft | Eixo: ${String(bearing).padStart(3, \u00270\u0027)}`;\\n                \\n                rulerPolyline.setTooltipContent(labelText);\"},{\"AllowMultiple\":false,\"EndLine\":2479,\"ReplacementContent\":\"            const bearing = getBearing(startPt, endPt);\\n            \\n            const dec = parseFloat(document.getElementById(\u0027mag-declination\u0027) ? document.getElementById(\u0027mag-declination\u0027).value : 0) || 0;\\n            const magBearing = Math.round((bearing - dec + 360) % 360);\\n            \\n            let labelText = `${Math.round(totalMeters)} m / ${Math.round(totalFt)} ft | Eixo: ${String(magBearing).padStart(3, \u00270\u0027)}° (MAG)`;\",\"StartLine\":2477,\"TargetContent\":\"            const bearing = getBearing(startPt, endPt);\\n            \\n            let labelText = `${Math.round(totalMeters)} m / ${Math.round(totalFt)} ft | Eixo: ${String(bearing).padStart(3, \u00270\u0027)}`;\"}]","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Modifying `script.js` to subtract the magnetic declination from the ruler\u0027s bearing, correctly displaying the Magnetic Proa as requested\"","toolSummary":"\"Update ruler magnetic heading\""}