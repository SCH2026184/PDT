{"AllowMultiple":"false","Description":"\"Corrigindo o cálculo do activeTelemetryIndex para não depender da visibilidade do painel. Se houver qualquer emprego XML carregado, o índice ativo é sempre o do select (padrão 0), evitando renderização dupla com XML de emprego único.\"","EndLine":"1083","Instruction":"\"Fix activeTelemetryIndex to always use the select value when parsedXMLReleases has items\"","ReplacementContent":"\"        // O índice ativo é sempre o do select quando há empregos XML carregados.\\n        // Não depende da visibilidade do painel (que some quando há só 1 emprego).\\n        const activeTelemetryIndex = parsedXMLReleases.length \u003e 0 ? parseInt(telemetryReleaseSelect.value || 0) : -1;\\n        \\n        parsedXMLReleases.forEach((rel, index) =\u003e {\\n            if (index === activeTelemetryIndex) return; // Já é renderizado pelo formulário acima\\n\"","StartLine":"1080","TargetContent":"\"        const activeTelemetryIndex = telemetryReleaseGroup \u0026\u0026 telemetryReleaseGroup.style.display !== \u0027none\u0027 ? parseInt(telemetryReleaseSelect.value) : -1;\\n        \\n        parsedXMLReleases.forEach((rel, index) =\u003e {\\n            if (index === activeTelemetryIndex) return; // Já é renderizado pelo formulário de designação acima\\n\"","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Fixing activeTelemetryIndex calculation in drawImpactsOnMap to prevent duplicate rendering when XML has only one AG_Release\"","toolSummary":"\"Fix duplicate employment rendering\""}