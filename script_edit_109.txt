{"AllowMultiple":"false","Description":"\"Igualando os cálculos matemáticos dos erros AL (Longitudinal) e AC (Lateral) do loop secundário de renderização XML aos cálculos principais da Designação. Isso garante que a posição projetada seja idêntica quer o emprego esteja selecionado ou apenas visível em segundo plano.\"","EndLine":"1123","Instruction":"\"Align secondary release loop error projection math with primary designation projection math\"","ReplacementContent":"\"            // Mapeando erro (AL = Longitudinal, AC = Lateral)\\n            const rLongErrM = rel.calcMisAL * 0.3048; // AL = Longitudinal\\n            const rLatErrM = rel.calcMisAC * 0.3048;  // AC = Lateral\\n            \\n            const rRad = (rHeadingDeg * Math.PI / 180);\\n            const rLatLong = (rLongErrM * Math.cos(rRad)) / 111111;\\n            const rLngLong = (rLongErrM * Math.sin(rRad)) / (111111 * Math.cos(rLatDeg * Math.PI / 180));\\n            \\n            const rLatHead = (rHeadingDeg + 90) % 360;\\n            const rdLatLat = (rLatErrM * Math.cos(rLatHead * Math.PI / 180)) / 111111;\\n            const rdLngLat = (rLatErrM * Math.sin(rLatHead * Math.PI / 180)) / (111111 * Math.cos(rLatDeg * Math.PI / 180));\\n            \\n            const rEffectedCenter = L.latLng(rLatDeg + rLatLong + rdLatLat, rLngDeg + rLngLong + rdLngLat);\"","StartLine":"1112","TargetContent":"\"            // Mapeando erro (AL = Longitudinal, AC = Lateral)\\n            const rLatErrM = rel.calcMisAL * 0.3048; // Assumindo AL/AC em ft no XML, convertendo para M? Wait, no formulário é pé, na função draw é metros\\n            const rLatHead = (rHeadingDeg + 180) % 360;\\n            const rLatLong = (rLatErrM * Math.cos(rLatHead * Math.PI / 180)) / 111111;\\n            const rLngLong = (rLatErrM * Math.sin(rLatHead * Math.PI / 180)) / (111111 * Math.cos(rLatDeg * Math.PI / 180));\\n\\n            const rLngErrM = rel.calcMisAC * 0.3048;\\n            const rLngHead = (rHeadingDeg - 90 + 360) % 360;\\n            const rdLatLat = (rLngErrM * Math.cos(rLngHead * Math.PI / 180)) / 111111;\\n            const rdLngLat = (rLngErrM * Math.sin(rLngHead * Math.PI / 180)) / (111111 * Math.cos(rLatDeg * Math.PI / 180));\\n            \\n            const rEffectedCenter = L.latLng(rLatDeg + rLatLong + rdLatLat, rLngDeg + rLngLong + rdLngLat);\"","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Fixing error projection math in secondary release loop\"","toolSummary":"\"Align secondary loop math\""}