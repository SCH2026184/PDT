{"ArtifactMetadata":"{\"ArtifactType\":\"task\",\"RequestFeedback\":false,\"Summary\":\"Task list para implementação do Undo/Redo\"}","Description":"\"Implement historyManager and bind Undo/Redo buttons\"","Instruction":"\"Insert historyManager, bind buttons and keyboard shortcuts\"","ReplacementChunks":"[{\"AllowMultiple\":false,\"EndLine\":68,\"ReplacementContent\":\"    const dmpiTargetsCache = {};\\n    let currentDmpiId = \u00271\u0027;\\n    let lastMapMouseLatLng = null;\\n\\n    // HISTORY MANAGER (Undo / Redo)\\n    const historyManager = {\\n        past: [],\\n        future: [],\\n        isRestoring: false,\\n\\n        captureState: function() {\\n            return {\\n                targetLatLng: targetLatLng ? { lat: targetLatLng.lat, lng: targetLatLng.lng } : null,\\n                designationLatLng: designationLatLng ? { lat: designationLatLng.lat, lng: designationLatLng.lng } : null,\\n                targetPolygonPoints: targetPolygonPoints ? JSON.parse(JSON.stringify(targetPolygonPoints)) : null,\\n                drawnTargetPoints: drawnTargetPoints ? drawnTargetPoints.map(p =\u003e ({lat: p.lat, lng: p.lng})) : [],\\n                savedTgts: savedTgts.map(t =\u003e ({\\n                    id: t.id,\\n                    name: t.name,\\n                    coord: t.coord,\\n                    latlng: { lat: t.latlng.lat, lng: t.latlng.lng },\\n                    elevation: t.elevation\\n                }))\\n            };\\n        },\\n\\n        saveState: function() {\\n            if (this.isRestoring) return;\\n            this.past.push(this.captureState());\\n            this.future = [];\\n            // Limita histórico a 50 ações\\n            if (this.past.length \u003e 50) this.past.shift();\\n        },\\n\\n        undo: function() {\\n            if (this.past.length === 0) return;\\n            this.isRestoring = true;\\n            this.future.push(this.captureState());\\n            this.applyState(this.past.pop());\\n            this.isRestoring = false;\\n        },\\n\\n        redo: function() {\\n            if (this.future.length === 0) return;\\n            this.isRestoring = true;\\n            this.past.push(this.captureState());\\n            this.applyState(this.future.pop());\\n            this.isRestoring = false;\\n        },\\n\\n        applyState: function(state) {\\n            // 1. Restore Main Target\\n            if (state\n\u003ctruncated 6489 bytes\u003e","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","TargetLintErrorIds":"[]","toolAction":"\"Implementing historyManager in script.js\"","toolSummary":"\"Add historyManager to script.js\""}