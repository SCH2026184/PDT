{"AllowMultiple":"false","Description":"\"Define radPerp inside drawEmploymentSet to fix ReferenceError\"","EndLine":"1301","Instruction":"\"Insert radPerp declaration before calculating twin centers\"","ReplacementContent":"\"            // Calcula os dois centros com afastamento de 2m para cada lado (espaçamento total de 4m)\\n            const radPerp = ((heading + 90) % 360) * Math.PI / 180;\\n            const shiftDist = 2; // metros\\n            const dLat1 = (-shiftDist * Math.cos(radPerp)) / 111111;\\n            const dLng1 = (-shiftDist * Math.sin(radPerp)) / (111111 * Math.cos(center.lat * Math.PI / 180));\\n            const center1 = L.latLng(center.lat + dLat1, center.lng + dLng1);\"","StartLine":"1297","TargetContent":"\"            // Calcula os dois centros com afastamento de 2m para cada lado (espaçamento total de 4m)\\n            const shiftDist = 2; // metros\\n            const dLat1 = (-shiftDist * Math.cos(radPerp)) / 111111;\\n            const dLng1 = (-shiftDist * Math.sin(radPerp)) / (111111 * Math.cos(center.lat * Math.PI / 180));\\n            const center1 = L.latLng(center.lat + dLat1, center.lng + dLng1);\"","TargetFile":"\"c:\\\\Users\\\\Diego Schroeder\\\\Desktop\\\\Antigravity\\\\PDE II\\\\script.js\"","toolAction":"\"Fixing radPerp reference error\"","toolSummary":"\"Fix reference error\""}